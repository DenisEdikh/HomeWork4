package aston.repository;

import aston.model.LogMethod;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogMethodRepository extends JpaRepository<LogMethod, Long> {
}
